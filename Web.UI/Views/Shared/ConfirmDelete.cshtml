@model DeleteConfirmInput
<div class="delDiv">
    @using (Html.BeginForm())
    {
        @Html.Raw(Model.Message)
    }
    @*used by the popupform .OnLoad func*@
    <input type="hidden" name="key" value="@(Model.Id)" />
</div>